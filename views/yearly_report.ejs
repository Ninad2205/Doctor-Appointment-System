<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Yearly Report - <%= yearLabel %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
      body { font-family: Inter, sans-serif; padding: 24px; background: #f7fafc; }
      .container { max-width: 1100px; margin: 0 auto; }
      .report-header { display:flex; justify-content:space-between; align-items:center; margin-bottom:16px }
      .report-title { font-size:1.4rem; font-weight:700 }
      .empty-state { text-align:center; padding:40px; color:#6b7280 }
      .table-modern { background:#fff; border-radius:8px; overflow:hidden; box-shadow:0 6px 18px rgba(0,0,0,0.06) }
      .back-link { text-decoration:none; color:#0b4fb3 }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="report-header">
        <div>
          <a href="/doctor-dashboard" class="back-link"><i class="fas fa-arrow-left"></i> Back to Dashboard</a>
          <h1 class="report-title">Appointments for <%= yearLabel %></h1>
          <div class="text-muted">Summary for the year</div>
        </div>
        <div>
          <button class="btn btn-secondary me-2" onclick="printReport()"><i class="fas fa-print"></i> Print</button>
          <a class="btn btn-primary" href="/doctor-dashboard">Close</a>
        </div>
      </div>

      <% if (appointments && appointments.length > 0) { %>
      <div id="printable" class="table-modern">
        <table class="table">
          <thead class="table-light">
            <tr>
              <th>Patient Name</th>
              <th>Contact</th>
              <th>Appointment Time</th>
              <th>Status</th>
            </tr>
          </thead>
          <tbody>
            <% appointments.forEach(function(a) { %>
            <tr>
              <td><%= a.patient_name %></td>
              <td><%= a.contact_no %></td>
              <td><%= new Date(a.appointment_time).toLocaleString() %></td>
              <td>
                <% if (a.status === 'Accepted') { %>
                  <span class="badge bg-success">Accepted</span>
                <% } else if (a.status === 'Rejected') { %>
                  <span class="badge bg-danger">Rejected</span>
                <% } else { %>
                  <span class="badge bg-warning text-dark">Pending</span>
                <% } %>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <% } else { %>
      <div class="empty-state">
        <i class="fas fa-calendar-times" style="font-size:48px; color:#cbd5e1"></i>
        <h4 class="mt-3">No Appointments This Year</h4>
        <p>There are no appointments scheduled for this year.</p>
      </div>
      <% } %>
    </div>
    <script>
      function printReport() { window.print(); }
    </script>
  </body>
</html>
